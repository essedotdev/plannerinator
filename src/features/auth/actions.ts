"use server";

/**
 * Auth Server Actions
 *
 * Note: User registration and authentication are handled by Better Auth.
 * Use the auth client methods from @/lib/auth-client:
 * - signUp.email() for registration
 * - signIn.email() for login
 * - signOut() for logout
 *
 * This file can be used for custom auth-related server actions if needed.
 */
